#!/usr/bin/env bash
# Stub only — do not invent embeddings without GEMINI_API_KEY.
# Nightly backfill of meals.embedding is out of scope for the 2026-09-14 audit PR.
set -euo pipefail
echo "embed_meals_backfill: requires GEMINI_API_KEY and a real meals.embedding writer."
echo "Not implemented. See docs/E2E_MARKET_AUDIT_2026-09-14.md (AI/ML)."
exit 1
