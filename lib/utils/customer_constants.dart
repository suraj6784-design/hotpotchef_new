// lib/utils/customer_constants.dart

import 'package:flutter/material.dart';
import 'app_theme.dart';

// 🚀 Centralized aliases mapping directly to AppTheme to prevent styling drift
const Color brandPrimary = AppTheme.primary;
const Color brandGradientEnd = AppTheme.primaryGradientEnd;
const Color appBackground = AppTheme.background;
const Color textMain = AppTheme.textMain;
const Color textMuted = AppTheme.textMuted;